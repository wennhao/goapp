# Docker environment variables
# ALLEEN voor Docker! Voor development mode (npm run dev) maak .env files in frontend/ en dashboard/ folders
# Copy this file to .env and adjust values as needed

# Backend URL for frontend (use server IP for remote access)
BACKEND_URL=http://localhost:3001

# Backend WebSocket URL for dashboard (use server IP for remote access)
BACKEND_WS_URL=ws://localhost:3001

# Example for remote access:
# BACKEND_URL=http://192.168.1.10:3001
# BACKEND_WS_URL=ws://192.168.1.10:3001
