# LXDE Autostart Configuration for GoApp Kiosk Mode
# Place this file in: ~/.config/lxsession/LXDE-pi/autostart
# 
# Instructions:
# 1. mkdir -p ~/.config/lxsession/LXDE-pi
# 2. Copy this file to ~/.config/lxsession/LXDE-pi/autostart
# 3. Make kiosk.sh executable: chmod +x ~/kiosk.sh
# 4. Reboot: sudo reboot

# Standard LXDE components
@lxpanel --profile LXDE-pi
@pcmanfm --desktop --profile LXDE-pi

# Disable screensaver and power management
@xset s off
@xset -dpms
@xset s noblank

# Hide mouse cursor after inactivity
@unclutter -idle 0.1 -root

# Launch GoApp in kiosk mode
@/home/pi/kiosk.sh
